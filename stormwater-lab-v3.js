(function(root){
  const RAINFALL_PRESETS={standard:{depth:58.7,duration:1,returnPeriod:2},heavy:{depth:76,duration:1,returnPeriod:5}};
  const PARAMS={nativeKsat:2.5,rainGarden:{mediaDepth:600,mediaKsat:180,sandDepth:100,gravelDepth:200},paving:{pavementDepth:102,gravelDepth:457,pavementVoid:0.12,gravelVoid:0.75},greenRoof:{mediaDepth:102,mediaKsat:254,porosity:0.45}};
  const dictionaries={
    th:{navCalc:'เครื่องคำนวณ',navMethod:'วิธีคำนวณ',eyebrow:'เครื่องมือทดลองสำหรับบ้านในประเทศไทย',heroTitle:'การรับมือน้ำท่วม<br>เริ่มได้จากพื้นที่บ้านของเรา',heroText:'ดูว่าบ้านของคุณสร้างน้ำไหลบ่าเท่าไร และการปรับพื้นที่เล็ก ๆ สามารถช่วยลดและชะลอน้ำที่ต้องระบายออกในช่วงฝนหนักได้อย่างไร',heroNote:'รุ่น Beta นี้เปรียบเทียบสมดุลน้ำของเหตุการณ์ฝน ไม่ได้ทำนายว่าพื้นที่จะน้ำท่วมหรือไม่',flowStore:'เก็บ',flowSoak:'ซึม',flowSlow:'ชะลอ',step1:'พื้นที่บ้านของคุณ',step1Sub:'แบ่งพื้นที่ทั้งหมดให้ครบ 100%',siteArea:'พื้นที่ดินทั้งหมด (m²)',totalAllocation:'รวมการจัดสรรพื้นที่',step2:'ฝนที่ต้องการทดสอบ',step2Sub:'Bangkok · เลือก preset หรือกำหนดเอง',rainDataTitle:'Bangkok Beta rainfall presets',rainDataText:'อ้างอิงข้อมูลฝนออกแบบของกรุงเทพมหานคร',standardRain:'ฝนออกแบบมาตรฐาน',heavyRain:'ฝนหนัก',advanced:'กำหนดเอง',rainDepth:'ปริมาณฝนทดสอบ (mm)',advancedDetails:'รายละเอียดฝน',duration:'ระยะเวลา (ชั่วโมง)',returnPeriod:'คาบอุบัติซ้ำ (ปี)',advancedNote:'ระยะเวลา 1 ชั่วโมงเป็น preset เพื่อเปรียบเทียบแนวคิดเบื้องต้น ไม่ได้หมายความว่า BMA กำหนดให้บ้านทุกหลังต้องใช้ฝน 1 ชั่วโมง',rainFalling:'ฝนที่ตกบนพื้นที่',step3:'ปรับพื้นที่ของคุณ',step3Sub:'วิธีจัดการน้ำฝน / Stormwater Design Options',results:'ผลการทดลอง',eventBalance:'ภาพรวมของฝนครั้งนี้',rainProperty:'ฝนที่ตกบนพื้นที่',currentDesign:'แบบปัจจุบัน',proposedDesign:'แบบทดลอง',baselineRunoff:'น้ำไหลบ่าที่ออกจากพื้นที่',proposedRunoff:'น้ำที่ออกระหว่างฝนที่ทดสอบ',estimatedReduction:'น้ำที่ไม่ต้องออกจากพื้นที่ทันที',balanceNote:'น้ำที่กักไว้ในชั้นวัสดุและน้ำที่หน่วงไว้ยังอยู่ในสมดุลน้ำ และอาจระบายออกภายหลัง เครื่องมือนี้ยังไม่คำนวณ peak flow หรืออัตราการไหล L/s',interpretEyebrow:'แปลผลให้เข้าใจง่าย',interpretTitle:'บ้านของคุณช่วยอะไรได้บ้าง?',broadMessage:'บ้านหนึ่งหลังไม่สามารถป้องกันน้ำท่วมทั้งเมืองได้ แต่การเก็บ ซึม และชะลอน้ำภายในพื้นที่ก่อนเข้าสู่ระบบระบายน้ำ สามารถช่วยลดภาระของระบบระบายน้ำในช่วงฝนหนักได้',methodEyebrow:'ความโปร่งใสของข้อมูล',methodTitle:'วิธีการคำนวณ',thaiData:'ข้อมูลฝน + ดิน + ภูมิอากาศ + พื้นที่ของไทย',simpleResults:'ผลลัพธ์ที่เจ้าของบ้านเข้าใจง่าย',thaiSource:'Bangkok / Thai source',thaiSourceText:'ฝน 58.7 mm (2 ปี, 1 ชั่วโมง) และ 76.0 mm (5 ปี, 1 ชั่วโมง) อ้างอิงข้อมูลฝนออกแบบของกรุงเทพมหานคร ส่วน Ksat ดินเดิม 2.5 mm/hr เป็นสมมติฐานนำร่องจากงานวิจัยชุดดินกรุงเทพฯ ไม่ใช่ค่าของทุกแปลง',tropicalReference:'Tropical reference',tropicalReferenceText:'สวนรับน้ำฝนใช้โปรไฟล์ bioretention: media 600 mm, media Ksat 180 mm/hr, transition sand 100 mm, gravel drainage 200 mm และ native-soil Ksat 2.5 mm/hr ไม่ใช่มาตรฐานกฎหมายไทย',internationalReference:'International prototype reference',internationalReferenceText:'พื้นซึมน้ำ: ชั้นพื้น 102 mm + gravel 457 mm, void 12%/75%. หลังคาเขียว: media 102 mm, Ksat 254 mm/hr, porosity 45%. เป็น EPA-derived prototype ไม่ใช่ข้อกำหนดก่อสร้างไทย',userInput:'User-supplied input',userInputText:'พื้นที่ดิน สัดส่วนพื้นผิว ขนาด/catchment ของมาตรการ ความจุว่างในถัง และค่าฝน Custom มาจากผู้ใช้ ค่าที่เกินพื้นที่จริงจะถูกจำกัดอัตโนมัติ',methodDetail:'Beta นี้เป็น event-based bucket water balance: 1 mm บน 1 m² = 1 L. ระบบคำนวณฝน การซึม การเก็บใช้ การกักในชั้นวัสดุ การหน่วง และ overflow แยกจากกัน โดยจัดลำดับพื้นที่เพื่อไม่ให้น้ำก้อนเดียวถูกนับซ้ำ สำหรับ Rain Garden น้ำที่ media รับได้จาก Ksat และความลึกของ media แต่ยังไม่ซึมลงดินเดิม จะแสดงเป็นน้ำกักชั่วคราวระหว่างเหตุการณ์ เพราะยังไม่มีข้อมูล outlet/time routing; จึงไม่ใช่การกำจัดน้ำถาวร ระยะยาวตั้งใจพัฒนาเป็น EPA SWMM ด้วยข้อมูลไทยที่ validate เพิ่มเติม',disclaimerTitle:'ขอบเขตของเครื่องมือ',disclaimerIntro:'เครื่องมือนี้ใช้เพื่อการเรียนรู้และเปรียบเทียบแนวทางออกแบบเบื้องต้นเท่านั้น โดยไม่ได้:',footer:'เครื่องมือเปรียบเทียบแนวคิดออกแบบเบื้องต้น',allocError:'พื้นที่รวมต้องเท่ากับ 100% จึงจะแสดงผลได้',noneSelected:'ยังไม่ได้เลือกวิธีจัดการน้ำฝน ผลลัพธ์จึงแสดงสมดุลน้ำของพื้นที่เดิม',summary:(v,r)=>`จากแบบที่คุณทดลอง น้ำประมาณ ${fmt(v)} ลิตรไม่จำเป็นต้องไหลออกจากพื้นที่ทันทีในช่วงเหตุการณ์ฝนนี้ โดยซึมลงดิน ${fmt(r.infiltrated)} ลิตร เก็บไว้ใช้ ${fmt(r.captured)} ลิตร กักไว้ในชั้นวัสดุ ${fmt(r.retained)} ลิตร และหน่วงไว้ชั่วคราว ${fmt(r.detained)} ลิตร`,labels:{roof:'หลังคา',hard:'ทางรถและพื้นทึบน้ำ',garden:'สวน / พื้นที่ภูมิทัศน์',permeable:'พื้นผิวซึมน้ำเดิม'},legend:{infiltrated:'น้ำซึมลงดิน',captured:'น้ำเก็บไว้ใช้',retained:'น้ำกักไว้ในชั้นวัสดุระหว่างเหตุการณ์',detained:'น้ำหน่วงไว้ชั่วคราว',leaving:'Overflow / น้ำที่ออกจากพื้นที่'},measures:{tank:['ถังเก็บน้ำฝน','เก็บน้ำฝนจากหลังคาไว้ใช้ภายหลัง และช่วยลดน้ำที่ต้องระบายออกทันที'],rainGarden:['สวนรับน้ำฝน','คำนวณเป็น engineered bioretention จากพื้นที่รับน้ำ ความสามารถรับน้ำของ media และการซึมลงดินเดิม'],paving:['พื้นซึมน้ำ','แทนที่พื้นทึบน้ำและคำนวณการซึม พื้นที่ว่างในชั้นพื้น/กรวด และ overflow'],detention:['ถังหน่วงน้ำ','รับน้ำในช่วงฝนหนักและค่อย ๆ ระบายออกภายหลัง ไม่ได้นำน้ำออกจากสมดุลน้ำถาวร'],greenRoof:['หลังคาเขียว','แทนที่หลังคาปกติและคำนวณน้ำกักใน media การระบายผ่านชั้นวัสดุ และ overflow']},contrib:{tank:(x)=>`เก็บน้ำไว้ใช้ ${fmt(x.captured)} ลิตร จาก storage ที่ว่างก่อนฝน ${fmt(x.availableStorage)} ลิตร และมี tank overflow ${fmt(x.overflow)} ลิตร`,rainGarden:(x)=>`รับน้ำ ${fmt(x.input)} ลิตร: ซึมลงดินเดิม ${fmt(x.infiltrated)} ลิตร กักไว้ชั่วคราวในระบบ bioretention ${fmt(x.stored)} ลิตร และ overflow ${fmt(x.overflow)} ลิตร น้ำที่กักไว้อาจระบายภายหลัง`,paving:(x)=>`พื้นที่ ${fmt(x.area)} m² ช่วยให้น้ำซึม ${fmt(x.infiltrated)} ลิตร กักในชั้นพื้นและกรวด ${fmt(x.stored)} ลิตร และเกิด overflow ${fmt(x.overflow)} ลิตร`,detention:(x)=>`หน่วงน้ำไว้ชั่วคราว ${fmt(x.detained)} ลิตร และ overflow ${fmt(x.overflow)} ลิตร น้ำที่หน่วงอาจระบายออกภายหลังและไม่ใช่ permanent runoff reduction`,greenRoof:(x)=>`พื้นที่ ${fmt(x.area)} m² กักน้ำใน media ${fmt(x.stored)} ลิตร ระบายผ่าน media ${fmt(x.drainage)} ลิตร และ overflow ${fmt(x.overflow)} ลิตร`}},
    en:{navCalc:'Calculator',navMethod:'Methodology',eyebrow:'A prototype tool for homes in Thailand',heroTitle:'Flood resilience<br>can start at home.',heroText:'See how much runoff your property generates — and how simple design changes can help reduce and slow down stormwater leaving your property during heavy rain.',heroNote:'This Beta compares an event water balance. It does not predict whether a property will flood.',flowStore:'Store',flowSoak:'Soak',flowSlow:'Slow',step1:'Your site',step1Sub:'Allocate the whole site to 100%',siteArea:'Total site area (m²)',totalAllocation:'Total allocation',step2:'Test rainfall',step2Sub:'Bangkok · Choose a preset or enter a custom event',rainDataTitle:'Bangkok Beta rainfall presets',rainDataText:'Based on Bangkok Metropolitan Administration design-rainfall information',standardRain:'Standard design rainfall',heavyRain:'Heavy rainfall',advanced:'Custom',rainDepth:'Test rainfall depth (mm)',advancedDetails:'Rainfall details',duration:'Duration (hours)',returnPeriod:'Return period (years)',advancedNote:'The 1-hour duration is a product preset for early comparison. It does not claim that BMA requires every home to use a 1-hour storm.',rainFalling:'Rain falling on property',step3:'Improve your site',step3Sub:'Stormwater Design Options / วิธีจัดการน้ำฝน',results:'Test results',eventBalance:'This rainfall event',rainProperty:'Rain falling on property',currentDesign:'Current design',proposedDesign:'Proposed design',baselineRunoff:'Baseline runoff leaving the site',proposedRunoff:'Leaving during the tested storm',estimatedReduction:'Not leaving the site immediately',balanceNote:'Water retained in material layers and water detained remain in the balance and may discharge later. This tool does not yet calculate peak flow or an L/s reduction.',interpretEyebrow:'Plain-language interpretation',interpretTitle:'What difference can your home make?',broadMessage:'One home cannot prevent flooding across a city. However, storing, infiltrating and slowing water on the property before it enters the drainage system can help reduce pressure on downstream drainage during heavy rainfall.',methodEyebrow:'Data transparency',methodTitle:'Methodology',thaiData:'Thai rainfall + soil + climate + site characteristics',simpleResults:'Simplified homeowner results',thaiSource:'Bangkok / Thai source',thaiSourceText:'Rainfall presets are 58.7 mm (2-year, 1-hour) and 76.0 mm (5-year, 1-hour), based on BMA design-rainfall information. Native-soil Ksat 2.5 mm/hr is a Bangkok pilot assumption from published soil-series research, not a value for every property.',tropicalReference:'Tropical reference',tropicalReferenceText:'Rain Garden bioretention profile: 600 mm media, media Ksat 180 mm/hr, 100 mm transition sand, 200 mm gravel drainage and native-soil Ksat 2.5 mm/hr. This is not a Thai regulatory standard.',internationalReference:'International prototype reference',internationalReferenceText:'Permeable pavement: 102 mm pavement + 457 mm gravel, with 12%/75% voids. Green roof: 102 mm media, Ksat 254 mm/hr and 45% porosity. These are EPA-derived prototypes, not Thai construction specifications.',userInput:'User-supplied input',userInputText:'Site area, land-cover shares, measure areas/catchments, tank storage available before the storm and Custom rainfall inputs come from the user. Entries above available areas are capped.',methodDetail:'This Beta is an event-based bucket water balance: 1 mm over 1 m² = 1 L. Rainfall, infiltration, reuse capture, material-layer retention, detention and overflow are kept separate, with source areas assigned in sequence to prevent double counting. For a Rain Garden, water admitted by the media Ksat and media depth but not infiltrated into native soil is reported as temporary event retention because outlet/time routing is not yet defined; it is not permanent removal. The longer-term architecture is intended to use EPA SWMM with additional validated Thai inputs.',disclaimerTitle:'Tool limitations',disclaimerIntro:'This is an educational, early-design comparison tool. It does not:',footer:'Educational early-design comparison tool',allocError:'Site allocation must total 100% before results can be shown.',noneSelected:'No stormwater design option is selected, so the result shows the existing-site water balance.',summary:(v,r)=>`In this test, about ${fmt(v)} litres does not need to leave the property immediately: ${fmt(r.infiltrated)} L infiltrates, ${fmt(r.captured)} L is captured for reuse, ${fmt(r.retained)} L remains in material layers and ${fmt(r.detained)} L is temporarily detained.`,labels:{roof:'Roof',hard:'Driveway / hard surface',garden:'Garden / landscape',permeable:'Existing permeable surface'},legend:{infiltrated:'Infiltrated into soil',captured:'Captured for reuse',retained:'Retained in material layers during event',detained:'Temporarily detained',leaving:'Overflow / discharge leaving the site'},measures:{tank:['Rainwater Tank','Stores roof water for later use and reduces water that needs to leave immediately.'],rainGarden:['Rain Garden','Modelled as engineered bioretention using its catchment, media intake and native-soil infiltration.'],paving:['Permeable Pavement','Replaces hard surface and calculates soil infiltration, pavement/gravel storage and overflow.'],detention:['Detention Tank','Receives water during heavy rain and releases it later. It does not permanently remove water from the balance.'],greenRoof:['Green Roof','Replaces normal roof and calculates media retention, media drainage and overflow.']},contrib:{tank:(x)=>`Captures ${fmt(x.captured)} L for reuse from ${fmt(x.availableStorage)} L of storage available before the storm, with ${fmt(x.overflow)} L of tank overflow.`,rainGarden:(x)=>`Receives ${fmt(x.input)} L: ${fmt(x.infiltrated)} L infiltrates native soil, ${fmt(x.stored)} L is temporarily retained in the bioretention system and ${fmt(x.overflow)} L overflows. Retained water may drain later.`,paving:(x)=>`${fmt(x.area)} m² infiltrates ${fmt(x.infiltrated)} L, stores ${fmt(x.stored)} L in pavement/gravel voids and overflows ${fmt(x.overflow)} L.`,detention:(x)=>`Temporarily detains ${fmt(x.detained)} L and overflows ${fmt(x.overflow)} L. Detained water may discharge later and is not a permanent runoff reduction.`,greenRoof:(x)=>`${fmt(x.area)} m² stores ${fmt(x.stored)} L in media, drains ${fmt(x.drainage)} L through the media and overflows ${fmt(x.overflow)} L.`}}
  };
  const disclaimer={th:['ทำนายว่าพื้นที่จะน้ำท่วมหรือไม่','จำลองระบบท่อระบายน้ำโดยรอบ','จำลองคลองหรือระดับน้ำท่วมภายนอก','คำนวณน้ำที่ไหลเข้าจากพื้นที่ข้างเคียง','คำนวณ peak-flow reduction หรืออัตราการไหล L/s','ใช้แทนวิศวกรโยธาหรือวิศวกรอุทกวิทยา','แสดงการผ่านข้อกำหนดทางกฎหมาย'],en:['predict whether a property will flood','model surrounding drainage networks','model canals or external flood levels','account for runoff entering from neighbouring properties','calculate peak-flow reduction or an L/s discharge rate','replace civil or hydrological engineering','demonstrate regulatory compliance']};
  const state={lang:'th',preset:'standard',siteArea:400,rainfall:58.7,duration:1,returnPeriod:2,alloc:{roof:35,hard:25,garden:30,permeable:10},enabled:{tank:false,rainGarden:false,paving:false,detention:false,greenRoof:false},tank:{capacity:5000,catchment:100,availabilityMode:'100',availablePct:100},rainGarden:{area:10,catchment:80,roof:true,hard:true},paving:{area:20},detention:{capacity:3000,catchment:80,source:'hard'},greenRoof:{area:20}};
  function n(v){v=Number(v);return Number.isFinite(v)&&v>0?v:0}
  function clamp(v,min,max){return Math.min(Math.max(n(v),min),Math.max(min,max))}
  function setAllocation(key,value){const next=clamp(value,0,100),previous=n(state.alloc[key]),delta=next-previous;state.alloc[key]=next;let remaining=Math.abs(delta);const preferred=key==='permeable'?['garden','hard','roof']:['permeable','garden','hard','roof'],order=preferred.filter(k=>k!==key);for(const k of order){if(remaining<=1e-9)break;if(delta>0){const change=Math.min(remaining,n(state.alloc[k]));state.alloc[k]-=change;remaining-=change}else if(delta<0){const change=Math.min(remaining,100-n(state.alloc[k]));state.alloc[k]+=change;remaining-=change}}const total=Object.values(state.alloc).reduce((sum,v)=>sum+n(v),0),correction=100-total;if(Math.abs(correction)>1e-9){const k=order.find(name=>state.alloc[name]+correction>=0&&state.alloc[name]+correction<=100);if(k)state.alloc[k]+=correction}}
  function fmt(v){return Math.round(Math.max(0,v||0)).toLocaleString('en-AU')}
  function nativeSurface(area,rain,duration){const input=area*rain,infiltrated=Math.min(input,area*PARAMS.nativeKsat*duration);return{input,infiltrated,runoff:input-infiltrated}}
  function allocateCatchment(request,roofAvail,hardAvail,useRoof,useHard){const max=(useRoof?roofAvail:0)+(useHard?hardAvail:0),take=Math.min(n(request),max);if(!take)return{roof:0,hard:0,total:0,max};if(useRoof&&useHard){const roof=take*(roofAvail/max);return{roof,hard:take-roof,total:take,max}}if(useRoof)return{roof:take,hard:0,total:take,max};if(useHard)return{roof:0,hard:take,total:take,max};return{roof:0,hard:0,total:0,max:0}}
  function setLanguage(s,lang){s.lang=lang==='en'?'en':'th';return s}
  function compute(s){
    const area=n(s.siteArea),rain=n(s.rainfall),duration=n(s.duration)||1,total=area*rain,allocTotal=Object.values(s.alloc).reduce((a,b)=>a+n(b),0),valid=Math.abs(allocTotal-100)<.001;
    const roof=area*n(s.alloc.roof)/100,hard=area*n(s.alloc.hard)/100,garden=area*n(s.alloc.garden)/100,permeable=area*n(s.alloc.permeable)/100;
    const baselineGarden=nativeSurface(garden,rain,duration),baselinePerm=nativeSurface(permeable,rain,duration),baselineInfiltrated=baselineGarden.infiltrated+baselinePerm.infiltrated,baselineLeaving=Math.max(0,total-baselineInfiltrated);
    if(!valid)return{valid,total,allocTotal,roof,hard,garden,permeable,baselineLeaving,infiltrated:0,captured:0,retained:0,detained:0,leaving:total,notLeaving:0,balanceError:0,details:{}};
    const greenArea=s.enabled.greenRoof?clamp(s.greenRoof.area,0,roof):0,pavingArea=s.enabled.paving?clamp(s.paving.area,0,hard):0,rgArea=s.enabled.rainGarden?clamp(s.rainGarden.area,0,garden):0;
    let roofAvail=roof-greenArea,hardAvail=hard-pavingArea,infiltrated=0,captured=0,retained=0,detained=0,leaving=0;
    const details={tank:{area:0,input:0,availableStorage:0,captured:0,overflow:0},rainGarden:{area:rgArea,catchment:0,input:0,infiltrated:0,stored:0,drainage:0,overflow:0},paving:{area:pavingArea,input:0,infiltrated:0,stored:0,overflow:0},detention:{area:0,input:0,detained:0,overflow:0},greenRoof:{area:greenArea,input:0,stored:0,drainage:0,overflow:0}};
    const gardenBalance=nativeSurface(garden-rgArea,rain,duration),permBalance=nativeSurface(permeable,rain,duration);infiltrated+=gardenBalance.infiltrated+permBalance.infiltrated;leaving+=gardenBalance.runoff+permBalance.runoff;
    if(greenArea){const x=details.greenRoof;x.input=greenArea*rain;const storage=greenArea*PARAMS.greenRoof.mediaDepth*PARAMS.greenRoof.porosity;x.stored=Math.min(x.input,storage);const after=x.input-x.stored,drainCap=greenArea*PARAMS.greenRoof.mediaKsat*duration;x.drainage=Math.min(after,drainCap);x.overflow=after-x.drainage;retained+=x.stored;leaving+=x.drainage+x.overflow}
    if(pavingArea){const x=details.paving;x.input=pavingArea*rain;x.infiltrated=Math.min(x.input,pavingArea*PARAMS.nativeKsat*duration);const after=x.input-x.infiltrated,storage=pavingArea*(PARAMS.paving.pavementDepth*PARAMS.paving.pavementVoid+PARAMS.paving.gravelDepth*PARAMS.paving.gravelVoid);x.stored=Math.min(after,storage);x.overflow=after-x.stored;infiltrated+=x.infiltrated;retained+=x.stored;leaving+=x.overflow}
    const tankLimit=roofAvail;
    if(s.enabled.tank){const x=details.tank;x.area=clamp(s.tank.catchment,0,roofAvail);roofAvail-=x.area;x.input=x.area*rain;x.availableStorage=n(s.tank.capacity)*clamp(s.tank.availablePct,0,100)/100;x.captured=Math.min(x.input,x.availableStorage);x.overflow=x.input-x.captured;captured+=x.captured;leaving+=x.overflow}
    const rgAllocation=allocateCatchment(s.rainGarden.catchment,roofAvail,hardAvail,!!s.rainGarden.roof,!!s.rainGarden.hard),rgLimit=rgAllocation.max;
    if(s.enabled.rainGarden){roofAvail-=rgAllocation.roof;hardAvail-=rgAllocation.hard;const x=details.rainGarden;x.catchment=rgAllocation.total;x.sourceRoof=rgAllocation.roof;x.sourceHard=rgAllocation.hard;x.input=rgArea*rain+rgAllocation.total*rain;x.intakeCapacity=rgArea*Math.min(PARAMS.rainGarden.mediaDepth,PARAMS.rainGarden.mediaKsat*duration);const accepted=Math.min(x.input,x.intakeCapacity);x.infiltrated=Math.min(accepted,rgArea*PARAMS.nativeKsat*duration);x.stored=accepted-x.infiltrated;x.drainage=0;x.overflow=x.input-accepted;infiltrated+=x.infiltrated;retained+=x.stored;leaving+=x.overflow}
    const detLimit=s.detention.source==='roof'?roofAvail:hardAvail;
    if(s.enabled.detention){const x=details.detention;x.area=clamp(s.detention.catchment,0,detLimit);x.source=s.detention.source;if(s.detention.source==='roof')roofAvail-=x.area;else hardAvail-=x.area;x.input=x.area*rain;x.detained=Math.min(x.input,n(s.detention.capacity));x.overflow=x.input-x.detained;detained+=x.detained;leaving+=x.overflow}
    leaving+=Math.max(0,roofAvail)*rain+Math.max(0,hardAvail)*rain;
    const accounted=infiltrated+captured+retained+detained+leaving,balanceError=total-accounted;if(Math.abs(balanceError)<1e-7)leaving+=balanceError;
    const notLeaving=Math.max(0,total-leaving),reductionVsBaseline=Math.max(0,baselineLeaving-leaving);
    return{valid,total,allocTotal,roof,hard,garden,permeable,baselineLeaving,infiltrated,captured,retained,detained,leaving,notLeaving,reductionVsBaseline,balanceError:total-(infiltrated+captured+retained+detained+leaving),details,limits:{greenRoof:roof,paving:hard,rainGardenArea:garden,tank:tankLimit,rainGardenCatchment:rgLimit,detention:detLimit}};
  }
  if(typeof module!=='undefined'&&module.exports){module.exports={compute,PARAMS,RAINFALL_PRESETS,setLanguage};return}
  const $=id=>document.getElementById(id),d=()=>dictionaries[state.lang],icons={tank:'▥',rainGarden:'✿',paving:'▦',detention:'↧',greenRoof:'⌂'};
  function renderAlloc(){const box=$('allocation');box.innerHTML='';Object.keys(state.alloc).forEach(k=>{const row=document.createElement('div');row.className='alloc-row';row.innerHTML=`<label>${d().labels[k]} <small id="sqm-${k}">${fmt(state.siteArea*state.alloc[k]/100)} m²</small></label><div class="percent-input"><input aria-label="${d().labels[k]} percentage" data-alloc="${k}" type="number" min="0" max="100" step="0.1" value="${state.alloc[k]}"><span>%</span></div><input aria-label="${d().labels[k]} slider" data-slider="${k}" type="range" min="0" max="100" step="0.1" value="${state.alloc[k]}">`;box.appendChild(row)});box.querySelectorAll('[data-alloc]').forEach(el=>el.oninput=e=>{setAllocation(e.target.dataset.alloc,e.target.value);renderAll(false)});box.querySelectorAll('[data-slider]').forEach(el=>el.oninput=e=>{setAllocation(e.target.dataset.slider,e.target.value);renderAll(false)})}
  function field(label,id,value,max,step=1){return `<label class="field"><span>${label}</span><input id="${id}" type="number" min="0" ${max!==undefined?`max="${max}"`:''} step="${step}" value="${value}"></label>`}
  function renderMeasures(){const m=d().measures,box=$('measures'),r=compute(state),roof=state.siteArea*state.alloc.roof/100,hard=state.siteArea*state.alloc.hard/100,garden=state.siteArea*state.alloc.garden/100,fragment=document.createDocumentFragment();const warn=id=>`<p id="${id}" class="validation hidden"></p>`;const availabilityLabel=state.lang==='th'?'พื้นที่ว่างในถังก่อนฝนตก':'Available storage before the storm';
    const availability=`<div class="tank-availability"><label class="field"><span>${availabilityLabel}</span><select id="tankAvailability"><option value="100" ${state.tank.availabilityMode==='100'?'selected':''}>100%</option><option value="75" ${state.tank.availabilityMode==='75'?'selected':''}>75%</option><option value="50" ${state.tank.availabilityMode==='50'?'selected':''}>50%</option><option value="25" ${state.tank.availabilityMode==='25'?'selected':''}>25%</option><option value="custom" ${state.tank.availabilityMode==='custom'?'selected':''}>${state.lang==='th'?'กำหนดเอง':'Custom'}</option></select></label>${state.tank.availabilityMode==='custom'?field(state.lang==='th'?'พื้นที่ว่าง (%)':'Available storage (%)','tankAvailablePct',state.tank.availablePct,100,1):'<div></div>'}</div>`;
    const bodies={tank:`${field(state.lang==='th'?'ความจุถัง (ลิตร)':'Tank capacity (litres)','tankCapacity',state.tank.capacity)}${field(state.lang==='th'?'พื้นที่หลังคาที่ต่อเข้าถัง (m²)':'Roof area connected (m²)','tankCatchment',state.tank.catchment,roof)}${availability}${warn('tankWarn')}<p class="note">${m.tank[1]}</p>`,rainGarden:`${field(state.lang==='th'?'พื้นที่สวนรับน้ำ (m²)':'Rain garden area (m²)','rgArea',state.rainGarden.area,garden)}${field(state.lang==='th'?'พื้นที่รับน้ำที่เชื่อมต่อ (m²)':'Connected catchment (m²)','rgCatchment',state.rainGarden.catchment,roof+hard)}<div class="source-checks full"><strong>${state.lang==='th'?'รับน้ำจาก':'Source'}:</strong><label><input id="rgRoof" type="checkbox" ${state.rainGarden.roof?'checked':''}>${d().labels.roof}</label><label><input id="rgHard" type="checkbox" ${state.rainGarden.hard?'checked':''}>${d().labels.hard}</label></div>${warn('rgAreaWarn')}${warn('rgWarn')}<p class="note">${m.rainGarden[1]}</p>`,paving:`${field(state.lang==='th'?'พื้นที่ทึบน้ำที่จะเปลี่ยน (m²)':'Hard surface to convert (m²)','pavingArea',state.paving.area,hard)}<div class="full"><p id="pavingBalance" class="note">${state.lang==='th'?`พื้นทึบน้ำ ${fmt(hard)} → ${fmt(Math.max(0,hard-r.details.paving.area))} m² · พื้นซึมน้ำเพิ่ม ${fmt(r.details.paving.area)} m²`:`Hard surface ${fmt(hard)} → ${fmt(Math.max(0,hard-r.details.paving.area))} m² · Permeable area +${fmt(r.details.paving.area)} m²`}</p>${warn('pavingWarn')}</div><p class="note">${m.paving[1]}</p>`,detention:`${field(state.lang==='th'?'ความจุหน่วงน้ำ (ลิตร)':'Storage volume (litres)','detCapacity',state.detention.capacity)}${field(state.lang==='th'?'พื้นที่รับน้ำที่เชื่อมต่อ (m²)':'Connected catchment (m²)','detCatchment',state.detention.catchment,roof+hard)}<label class="field full"><span>${state.lang==='th'?'แหล่งน้ำ':'Runoff source'}</span><select id="detSource"><option value="hard" ${state.detention.source==='hard'?'selected':''}>${d().labels.hard}</option><option value="roof" ${state.detention.source==='roof'?'selected':''}>${d().labels.roof}</option></select></label>${warn('detWarn')}<p class="note">${m.detention[1]}</p>`,greenRoof:`${field(state.lang==='th'?'พื้นที่หลังคาที่จะเปลี่ยน (m²)':'Roof area to convert (m²)','greenArea',state.greenRoof.area,roof)}<div class="full"><p id="greenPercent" class="note">${state.lang==='th'?`${fmt(r.details.greenRoof.area)} m² = ${roof?((r.details.greenRoof.area/roof)*100).toFixed(1):'0.0'}% ของพื้นที่หลังคา`:`${fmt(r.details.greenRoof.area)} m² = ${roof?((r.details.greenRoof.area/roof)*100).toFixed(1):'0.0'}% of total roof area`}</p>${warn('greenWarn')}<p class="note">${m.greenRoof[1]}</p></div>`};
    Object.keys(m).forEach(k=>{const item=document.createElement('div');item.className='measure';item.innerHTML=`<label class="measure-title"><input data-enable="${k}" type="checkbox" ${state.enabled[k]?'checked':''}><span class="measure-icon">${icons[k]}</span><span><strong>${m[k][0]}</strong><small>${state.enabled[k]?(state.lang==='th'?'กำลังใช้ในแบบทดลอง':'Included in proposal'):(state.lang==='th'?'เลือกเพื่อทดลอง':'Select to test')}</small></span></label>${state.enabled[k]?`<div class="measure-body">${bodies[k]}</div>`:''}`;fragment.appendChild(item)});box.replaceChildren(fragment);
    box.querySelectorAll('[data-enable]').forEach(el=>el.onchange=e=>{state.enabled[e.target.dataset.enable]=e.target.checked;renderAll()});const binds=[['tankCapacity',state.tank,'capacity'],['tankCatchment',state.tank,'catchment',()=>compute(state).limits.tank],['tankAvailablePct',state.tank,'availablePct',()=>100],['rgArea',state.rainGarden,'area',()=>compute(state).limits.rainGardenArea],['rgCatchment',state.rainGarden,'catchment',()=>compute(state).limits.rainGardenCatchment],['pavingArea',state.paving,'area',()=>state.siteArea*state.alloc.hard/100],['detCapacity',state.detention,'capacity'],['detCatchment',state.detention,'catchment',()=>compute(state).limits.detention],['greenArea',state.greenRoof,'area',()=>state.siteArea*state.alloc.roof/100]];binds.forEach(([id,obj,key,maxValue])=>{if($(id))$(id).oninput=e=>{obj[key]=n(e.target.value);if(maxValue)obj[key]=clamp(obj[key],0,maxValue());e.target.value=obj[key];renderResults();refreshMeasureDerived();renderMeasureValidation()}});if($('tankAvailability'))$('tankAvailability').onchange=e=>{state.tank.availabilityMode=e.target.value;if(e.target.value!=='custom')state.tank.availablePct=Number(e.target.value);renderAll()};if($('rgRoof'))$('rgRoof').onchange=e=>{state.rainGarden.roof=e.target.checked;renderAll()};if($('rgHard'))$('rgHard').onchange=e=>{state.rainGarden.hard=e.target.checked;renderAll()};if($('detSource'))$('detSource').onchange=e=>{state.detention.source=e.target.value;renderAll()};renderMeasureValidation()}
  function refreshMeasureDerived(){const r=compute(state),hard=state.siteArea*state.alloc.hard/100,roof=state.siteArea*state.alloc.roof/100;if($('pavingBalance'))$('pavingBalance').textContent=state.lang==='th'?`พื้นทึบน้ำ ${fmt(hard)} → ${fmt(Math.max(0,hard-r.details.paving.area))} m² · พื้นซึมน้ำเพิ่ม ${fmt(r.details.paving.area)} m²`:`Hard surface ${fmt(hard)} → ${fmt(Math.max(0,hard-r.details.paving.area))} m² · Permeable area +${fmt(r.details.paving.area)} m²`;if($('greenPercent'))$('greenPercent').textContent=state.lang==='th'?`${fmt(r.details.greenRoof.area)} m² = ${roof?((r.details.greenRoof.area/roof)*100).toFixed(1):'0.0'}% ของพื้นที่หลังคา`:`${fmt(r.details.greenRoof.area)} m² = ${roof?((r.details.greenRoof.area/roof)*100).toFixed(1):'0.0'}% of total roof area`}
  function renderMeasureValidation(){const r=compute(state),checks=[['tankWarn',state.tank.catchment>(r.limits?.tank||0),r.limits?.tank||0],['rgAreaWarn',state.rainGarden.area>(r.limits?.rainGardenArea||0),r.limits?.rainGardenArea||0],['rgWarn',state.rainGarden.catchment>(r.limits?.rainGardenCatchment||0),r.limits?.rainGardenCatchment||0],['pavingWarn',state.paving.area>(r.limits?.paving||0),r.limits?.paving||0],['detWarn',state.detention.catchment>(r.limits?.detention||0),r.limits?.detention||0],['greenWarn',state.greenRoof.area>(r.limits?.greenRoof||0),r.limits?.greenRoof||0]];checks.forEach(([id,bad,max])=>{const el=$(id);if(!el)return;el.textContent=state.lang==='th'?`ค่าที่กรอกเกินพื้นที่ต้นทางที่ยังเหลือ ระบบจึงจำกัดผลคำนวณไว้ที่ ${fmt(max)} m²`:`The entry exceeds its remaining source area, so the calculation is capped at ${fmt(max)} m².`;el.classList.toggle('hidden',!bad)})}
  function renderResults(){const r=compute(state),t=d();$('allocationTotal').textContent=`${fmt(state.siteArea)} m² / ${Number(r.allocTotal.toFixed(1))}%`;$('allocationTotal').className=r.valid?'good':'bad';$('allocationError').textContent=t.allocError;$('allocationError').classList.toggle('hidden',r.valid);$('rainVolume').textContent=`${fmt(r.total)} L`;$('rainFormula').textContent=`${Number(state.siteArea).toLocaleString('en-AU')} m² × ${Number(state.rainfall).toLocaleString('en-AU')} mm`;$('resRain').textContent=`${fmt(r.total)} L`;$('resCurrent').textContent=r.valid?`${fmt(r.baselineLeaving)} L`:'—';$('resProposed').textContent=r.valid?`${fmt(r.leaving)} L`:'—';$('resReduction').textContent=r.valid?`${fmt(r.notLeaving)} L`:'—';const parts=[['infiltrated',r.infiltrated,'bar-retained'],['captured',r.captured,'bar-captured'],['retained',r.retained,'bar-retained'],['detained',r.detained,'bar-detained'],['leaving',r.leaving,'bar-leaving']];$('balanceBar').innerHTML=parts.map(([k,v,c])=>`<span class="${c}" style="width:${r.total?v/r.total*100:0}%"></span>`).join('');$('legend').innerHTML=parts.map(([k,v,c])=>`<div class="legend-row"><i class="${c}"></i><span>${t.legend[k]}</span><strong>${fmt(v)} L</strong></div>`).join('');$('interpretSummary').textContent=r.valid?t.summary(r.notLeaving,r):t.allocError;const selected=Object.keys(state.enabled).filter(k=>state.enabled[k]);$('contributions').innerHTML=selected.length?selected.map(k=>`<div class="contribution"><strong>${t.measures[k][0]}</strong><p>${t.contrib[k](r.details[k])}</p></div>`).join(''):`<div class="contribution"><p>${t.noneSelected}</p></div>`}
  function renderStatic(){document.documentElement.lang=state.lang;document.querySelectorAll('[data-i18n]').forEach(el=>{const key=el.dataset.i18n;if(d()[key]!==undefined)el.innerHTML=d()[key];else if(key==='longTermEngine')el.textContent=state.lang==='th'?'ระยะยาว: EPA SWMM':'Long term: EPA SWMM';else if(key==='initialStorageNote')el.textContent=state.lang==='th'?'สำหรับการเปรียบเทียบ Beta ชั้นกักเก็บของพื้นซึมน้ำและหลังคาเขียวถือว่ามีพื้นที่ว่างเต็มที่ก่อนฝนเริ่ม ส่วนถังเก็บน้ำใช้ค่าพื้นที่ว่างที่ผู้ใช้เลือก':'For this Beta comparison, permeable-pavement and green-roof storage layers are assumed empty at the start of the event; the rainwater tank uses the available-storage setting selected by the user.'});$('thBtn').classList.toggle('active',state.lang==='th');$('enBtn').classList.toggle('active',state.lang==='en');$('disclaimerList').innerHTML=disclaimer[state.lang].map(x=>`<li>${x}</li>`).join('');['standard','heavy','custom'].forEach(k=>$(k+'Btn').classList.toggle('active',state.preset===k))}
  function renderAll(full=true){renderStatic();if(full)renderAlloc();else{document.querySelectorAll('[data-alloc]').forEach(el=>el.value=state.alloc[el.dataset.alloc]);document.querySelectorAll('[data-slider]').forEach(el=>el.value=state.alloc[el.dataset.slider]);Object.keys(state.alloc).forEach(k=>{const e=$(`sqm-${k}`);if(e)e.textContent=`${fmt(state.siteArea*state.alloc[k]/100)} m²`})}renderMeasures();renderResults();$('duration').value=state.duration;$('returnPeriod').value=state.returnPeriod||'';$('rainfall').value=state.preset==='standard'||state.preset==='heavy'?Number(state.rainfall).toFixed(1):state.rainfall}
  function setPreset(preset){state.preset=preset;const p=RAINFALL_PRESETS[preset];if(p){state.rainfall=p.depth;state.duration=p.duration;state.returnPeriod=p.returnPeriod}renderAll()}
  $('siteArea').oninput=e=>{state.siteArea=n(e.target.value);renderAll()};$('rainfall').oninput=e=>{state.rainfall=n(e.target.value);state.preset='custom';renderStatic();renderResults()};$('duration').oninput=e=>{state.duration=n(e.target.value)||1;state.preset='custom';renderStatic();renderResults()};$('returnPeriod').oninput=e=>{state.returnPeriod=n(e.target.value);state.preset='custom';renderStatic();renderResults()};$('standardBtn').onclick=()=>setPreset('standard');$('heavyBtn').onclick=()=>setPreset('heavy');$('customBtn').onclick=()=>setPreset('custom');$('thBtn').onclick=()=>{setLanguage(state,'th');renderAll()};$('enBtn').onclick=()=>{setLanguage(state,'en');renderAll()};renderAll();
})(typeof window!=='undefined'?window:globalThis);
